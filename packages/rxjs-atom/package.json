{
  "name": "@rixio/rxjs-atom",
  "version": "0.2.1",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": [
    "/build"
  ],
  "keywords": [
    "frp",
    "lens",
    "reactive",
    "rx",
    "rxjs",
    "atom"
  ],
  "scripts": {
    "build": "npm run lint && npm run build-lib",
    "build-lib": "rimraf ./build && tsc --project tsconfig-build.json && npm run copy-assets",
    "copy-json-to-lib": "cpx \"./src/**/*.json\" ./build",
    "copy-assets": "npm run copy-json-to-lib",
    "test": "jest --silent",
    "lint:js": "eslint --ext .js,.jsx,.ts,.tsx ./",
    "lint:types": "tsc --noEmit",
    "lint": "npm run lint:js && npm run lint:types",
    "lint:fix": "eslint --ext .js,.ts,.ts,.tsx ./ --fix --quiet",
    "ci": "sh ./scripts/ci.sh",
    "release": "sh ./scripts/release.sh"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/roborox/rixio.git"
  },
  "devDependencies": {
    "@rixio/lens": "^0.2.1",
    "@roborox/eslint-config-default": "2.2.0",
    "@types/jest": "26.0.0",
    "eslint": "7.10.0",
    "jest": "26.4.2",
    "rxjs": "6.5.5",
    "ts-jest": "26.4.1",
    "typescript": "4.0.3"
  },
  "peerDependencies": {
    "@rixio/lens": ">= 0.0.1",
    "rxjs": ">= 6.5.0 < 7.0.0-0"
  }
}
