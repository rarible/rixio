{
  "name": "@rixio/lens",
  "version": "0.2.1",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": [
    "/build"
  ],
  "keywords": [
    "frp",
    "lens",
    "reactive"
  ],
  "scripts": {
    "build": "rimraf ./build && tsc --project tsconfig-build.json && npm run copy-assets",
    "copy-json-to-lib": "cpx \"./src/**/*.json\" ./build",
    "copy-assets": "npm run copy-json-to-lib",
    "test": "jest --silent",
    "verify": "tsc --noEmit",
    "ci": "sh ./scripts/ci.sh",
    "release": "sh ./scripts/release.sh"
  },
  "publishConfig": {
    "access": "public"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/roborox/rixio.git"
  },
  "devDependencies": {
    "@types/jest": "26.0.14",
    "cpx": "1.5.0",
    "jest": "26.4.2",
    "ts-jest": "26.4.1",
    "typescript": "4.0.3"
  }
}
